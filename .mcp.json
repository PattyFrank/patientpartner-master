{
  "mcpServers": {
    "image-generator": {
      "command": "python",
      "args": ["mcp-servers/image-generator/server.py"],
      "env": {
        "REPLICATE_API_TOKEN": "${REPLICATE_API_TOKEN}"
      }
    }
  }
}
